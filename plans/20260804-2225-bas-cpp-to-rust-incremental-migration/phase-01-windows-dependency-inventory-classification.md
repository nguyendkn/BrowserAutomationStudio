# Phase 01: Windows-Dependency Inventory & Classification

## Context Links
- `research/researcher-01-build-environment.md` — macOS cannot build BAS today; unconditional `windows.h`/WinAPI in Engine + ChromeWorker.
- `scout/scout-01-module-map.md` — module-level WinAPI/COM file counts (Engine 31, ChromeWorker 43) at a coarser grep granularity than this phase's per-symbol scan.
- `plan.md` — Overview (Windows-first primary goal; cross-platform as non-gating follow-on), Phases table (this phase's "X-plat groundwork, non-gating" track), `bas-platform` crate definition.
- Coordinator-supplied full-repo scan (this phase's starting numbers, see Key Insights) — verified/corrected against the actual repo during this phase's execution (see Findings below).

## Overview
- **Priority:** P2 — parallel/background groundwork for the deferred macOS/Linux cross-platform hardening track (Phase 2). **Not the plan's starting gate and not a precondition for Phase 3 onward** — per the user's 2026-09-18 reprioritization, Windows is BAS's real, current, production platform (v25.8.0 ships today, builds fine there with the existing MSVC/Qt toolchain) and the plan's actual first milestone is standing up the Rust workspace and validating it on Windows (Phase 3 onward). This phase's classification work (below) still runs and is still valuable — it just doesn't block anything until Phase 2 (or later, whenever the hardening track is scheduled) actually needs it.
- **Status:** pending.
- **This phase writes NO code.** Pure investigation: classify every Windows-blocking call site repo-wide (not just Engine/ChromeWorker) into a fix category, and verify the standing assumptions (some already found WRONG during this plan's own drafting — see Findings) — feeds Phase 2 whenever that phase runs, not a gate on Phase 3.
- **This phase's supporting task — the Windows reference build — is promoted to the plan's PRIMARY validation target** (see Architecture/Related Code Files below): a real Windows build+launch, on a local dev machine or self-hosted Windows CI runner, not `windows-latest` GitHub Actions. **Currently blocked on this machine**: MSVC Build Tools 2022 is present, but there is no Qt install and no `basbuild`-provided dependency bundle (`$(BAS_PATH)`/`$(BAS_PATH_WORKER)`, referenced by every `.pro` file) — open environment blocker, see plan.md Unresolved Questions #3.

## Key Insights

**Starting numbers (coordinator's full-repo scan, used as given):**
- `windows.h` inclusion: ChromeWorker 36/320 files, Engine 13/760, WebInterfaceBrowser 5/16, Studio 3/42, SchedulerBrowser 2/5, Scheduler 2/47, ShopViewer 1/5, RemoteExecuteScript 1/56, Modules 2/26 (corrected — the coordinator's original count of 1 missed `Modules/Timezones/dll/maxminddb.c:20`, a vendored MaxMindDB C library file; the originally-tracked one is `Modules/UserNotification/dll/moduledll.cpp:3`). (Updater, FastExecuteScript, Tests: 0.)
- **`maxminddb.c`'s `windows.h` include is immediately followed by `#include <ws2ipdef.h>` (line 21) — a WinSock header, not just an incidental include.** This needs the same careful per-file classification as everything else in this phase, not an assumption either way — do not label it "trivial" without reading what it's actually used for (likely IP-address parsing for geolocation, given MaxMindDB's purpose; may be category (b) if a portable IP-parsing crate covers it, or (a) if the Windows-specific path is dead code on this vendored library's actual call sites in BAS).
- `_WIN32`/`WIN32` preprocessor guards: Engine 6, Studio 3, ChromeWorker 3, Scheduler 2, Updater 1, RemoteExecuteScript 1, Modules 1.
- CEF dependency in `.pro` files: WebInterfaceBrowser, SchedulerBrowser, ChromeWorker, Engine.

**Correction found during this plan's drafting (verify again at execution time, don't assume stale):** the coordinator's grep for "COM usage" used the pattern `CoCreateInstance|IUnknown|__declspec|CComPtr` and reported Modules 9 files / Engine 3 / Studio 1 / ChromeWorker 1, framing this as "the single highest-value architectural target" needing a COM-based plugin-loading redesign. A grep for the REAL COM APIs alone (`CoCreateInstance|QueryInterface|__uuidof|IID_|CComPtr|CoInitialize`, excluding `__declspec`), **scoped to C/C++ source (`*.cpp`/`*.h`/`*.hpp`/`*.cc`)**, returns **zero matches in any BAS-authored or vendored C/C++ file**. (An UNSCOPED repo-wide grep also hits 2 non-C++ assets that are not load-bearing for this classification: `ChromeWorker/html/toolbox/vs/basic-languages/src/cpp.js`, a bundled Monaco editor language file whose keyword list contains the string `__uuidof`, and `Modules/SQL/js/manifest.json`, a bundled Sequelize JS dependency whose code contains the string `QueryInterface` — both are third-party JS/JSON assets, not BAS C++ source, and don't affect any compile/link decision.) Every one of the flagged C/C++ files uses `__declspec(dllexport)`/`__declspec(dllimport)`/`__declspec(thread)` — plain MSVC export/import/TLS keywords, not COM. Evidence: `Solution/Modules/DateTime/dll/moduledll.h` (and 8 sibling `moduledll.h` files) export `extern "C"` functions via `__declspec(dllexport)` only; `Solution/Engine/oauth/o0export.h:7,9` and `Solution/Engine/snappy/snappy-c.h:38,40` are dllexport/dllimport macros; `Solution/Engine/debug_memory/c++11.h:300` is `__declspec(thread)` (TLS). **Reclassify Modules' export mechanism from category (d) to category (b)** — `__declspec(dllexport)` has a one-line portable equivalent (`#if defined(_WIN32) ... #else __attribute__((visibility("default"))) ... #endif`, or nothing at all — POSIX shared libraries export `extern "C"` symbols by default).
- **The actual plugin-LOADING mechanism (the other half of the "is this COM?" question) is already cross-platform.** `Solution/Engine/modulemanager.cpp` loads each Module DLL via Qt's `QLibrary` (`#include <QLibrary>` at line 9; `new QLibrary(...)`, `->load()`, `->resolve(...)` at lines 697-767) — `QLibrary` is Qt's own cross-platform dynamic-library abstraction (`.dll`/`.dylib`/`.so` transparently, backed by `LoadLibrary`/`dlopen` internally). There is no COM registration, no `LoadLibrary`/`GetProcAddress` call in `modulemanager.cpp` at all. The Modules plugin system does NOT need a `libloading`+`cxx` redesign — it already works cross-platform at the loader level; only the DLL-side export macro (category b, trivial) is Windows-specific.
- **Genuine Windows-only `LoadLibrary`/`GetProcAddress` calls DO exist in Engine, but unrelated to Modules**: `Solution/Engine/CrashHandler.cpp:166,206` loads `dbghelp.dll` for crash-dump generation (Windows-specific crash API — category c, no cross-platform equivalent, needs a `#[cfg(target_os = "windows")]` no-op/alternative on other OSes); `Solution/Engine/devicescalemanager.cpp:29,32` loads `Shcore.dll` for DPI-scaling (Windows-specific — category c). Both are small, isolated, 1 file each.
- **A separate genuine `OpenProcess`/`HANDLE` call site**, distinct from the `LoadLibrary` sites above: `Solution/Engine/webdriverprocesscomunicatorfactory.cpp:15,55` (`#include <Windows.h>`; `HANDLE process = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pid);`) — process-existence/query check, category (b), maps directly to the `sysinfo` crate's process-lookup API. Verified by reading the file directly; not to be confused with `processdeleter.cpp` (windows.h include is dead — body only calls portable `QProcess::kill()`/`deleteLater()`, category a) or `memoryinfo.cpp` (calls `GlobalMemoryStatusEx`, already `Q_OS_WIN`-guarded with a placeholder fallback, category b) — three different files, three different WinAPI stories, easy to conflate if classifying by filename-list alone instead of reading each file.
- **CEF has zero macOS build-config today**: grep across every `.pro` file in the repo for `macx`/`Q_OS_MAC`/`__APPLE__` returns nothing — no `.pro` file has ever had a mac branch. `ChromeWorker.pro:168`'s `LIBS +=` line unconditionally links `-lAdvapi32 -luser32 -lPsapi -lshell32 -lDbgHelp -lComdlg32 -lgdi32` (Windows system libs) alongside CEF in one unconditional line — CEF itself ships macOS binaries/frameworks, but BAS's qmake config has never been taught how to link against them. This is real work (a `macx { ... }` qmake block with mac-appropriate CEF framework paths + mac system libs), not a toggle — but it is qmake/build-config work, not a Rust-migration task.

## Requirements
- Functional: produce `docs/macos-blocker-classification.md` — a per-file (ideally per-call-site) table covering every `windows.h`/`_WIN32`/WinAPI-symbol hit across ALL modules (not just Engine/ChromeWorker), each row tagged with exactly one category: **(a) incidental/removable** (include with no real platform dependency — can just delete or leave unguarded since it's unused), **(b) trivially portable** (has a direct cross-platform crate/macro equivalent — `sysinfo` for process info, `std::net`/`tokio` for WinSock networking, a portable export macro for `__declspec(dllexport)`), **(c) genuinely OS-specific** (wrap behind a `#[cfg(target_os = "windows")]`-gated trait in the new `bas-platform` crate, with a real or no-op alternative for macOS/Linux), **(d) architecturally significant redesign needed** (reserved for a case actually proven to need one — Modules' export mechanism no longer qualifies per this phase's own correction above; re-verify no other (d) candidate exists before closing this phase).
- Non-functional: every category assignment must cite the exact `file:line` evidence (no category assigned by inference from a module-level count alone) — this phase's whole value is precision the coordinator's own file-count-only scan didn't have.
- Verify (don't assume) the CEF-macOS-SDK build-config item as a separate checklist track, independent of the Rust classification table.

## Architecture
- Deliverable: `docs/macos-blocker-classification.md`, one row per call site, columns: `module | file:line | symbol/API | category (a-d) | proposed fix (crate name / trait name / macro / n-a) | priority tier`.
- Priority tiers for Phase 2's implementation order, re-ranked from the coordinator's original "Modules first" ordering now that Modules' export-macro fix is trivial: **Tier 1 — ChromeWorker's 36 `windows.h` files** (largest real count, category b/c mix, and ChromeWorker is already this plan's designated cross-platform-blocker module per plan.md); **Tier 2 — Engine's 13 `windows.h` + the 2 genuine `LoadLibrary` sites** (`CrashHandler.cpp`, `devicescalemanager.cpp`) + the `OpenProcess`/`HANDLE` site (`webdriverprocesscomunicatorfactory.cpp`) + `memoryinfo.cpp`'s `GlobalMemoryStatusEx` fallback + Engine's `_WIN32` guards; **Tier 3 — Modules' TWO real `windows.h` files** (`Solution/Modules/UserNotification/dll/moduledll.cpp:3`, `Solution/Modules/Timezones/dll/maxminddb.c:20`) + the 9 `__declspec`-only export headers (cheap, mechanical, batch together); **Tier 4 — remaining smaller modules** (WebInterfaceBrowser 5, Studio 3, SchedulerBrowser 2, Scheduler 2, ShopViewer 1, RemoteExecuteScript 1).
- **The Windows reference build — now the plan's PRIMARY validation target, not a side task.** A real Windows build+launch (`qmake && nmake` against `Solution.pro` + the `SUBDIRS`-omitted `.pro` files individually — `ChromeWorker`, `WebInterfaceBrowser`, `SchedulerBrowser`, `Scheduler`, `Modules/*`, `Tests`, then a smoke-launch), run on a **local Windows dev machine or a self-hosted Windows CI runner** — explicitly NOT GitHub Actions `windows-latest` (resolved: interactive debugging weighed over hosted-CI reproducibility, plan.md Unresolved Questions #2). It serves two purposes now: (1) it IS the plan's baseline "Windows works" proof and the reference every Phase-3-onward Windows checkpoint diffs against; (2) it doubles as the pre-change behavior snapshot Phase 2's hardening track needs before touching any blocking call site, per this plan's characterization-tests-first rule.
- **Open environment blocker on that build:** this machine has MSVC Build Tools 2022 but NO Qt install (`qmake` not on PATH, no `C:/Qt`) and no `basbuild`-provided dependency bundle for `$(BAS_PATH)`/`$(BAS_PATH_WORKER)` — both must be provisioned before the reference build can run at all. Not resolved by this plan; see plan.md Unresolved Questions #3 and #5.

## Related Code Files
- `Solution/ChromeWorker/` — read/analyze all 36 `windows.h`-including files (exact list to be pulled via `grep -rl windows.h` at execution time; the 34-vs-36 count found during this plan's own spot-check is within normal grep-pattern variance, not material).
- `Solution/Engine/CrashHandler.cpp`, `Solution/Engine/devicescalemanager.cpp` — the two confirmed genuine `LoadLibrary` call sites (category c).
- `Solution/Engine/webdriverprocesscomunicatorfactory.cpp` — the confirmed genuine `OpenProcess`/`HANDLE` call site (category b, `sysinfo`-equivalent).
- `Solution/Engine/memoryinfo.cpp` — `GlobalMemoryStatusEx`, already `Q_OS_WIN`-guarded with a placeholder fallback (category b, upgrade to real `sysinfo`-backed value).
- `Solution/Engine/processdeleter.cpp` — dead `windows.h` include (category a, delete only; body already portable via `QProcess`).
- `Solution/Engine/modulemanager.cpp`, `Solution/Engine/imodulemanager.h` — confirmed `QLibrary`-based (already cross-platform) plugin loader; read-only reference, NOT a fix target.
- `Solution/Modules/UserNotification/dll/moduledll.cpp` — one of TWO Modules files with a real `windows.h` include.
- `Solution/Modules/Timezones/dll/maxminddb.c` — the SECOND Modules file with a real `windows.h` include (line 20, immediately followed by `ws2ipdef.h` at line 21 — a vendored MaxMindDB C library file, needs its own classification, not assumed trivial).
- `Solution/Modules/{DateTime,Timezones,Processes,FileSystem,ImageProcessing,FontPack,RegularExpression,UserNotification,CurlWrapper}/dll/moduledll.h` — the 9 `__declspec`-only export headers (category b, batch fix).
- `Solution/ChromeWorker/ChromeWorker.pro` — the CEF/mac build-config gap (`LIBS +=` line 168, no `macx` block anywhere in the repo).
- **Create:** `docs/macos-blocker-classification.md` — the classification deliverable.
- **Create (PRIMARY validation target):** local build script or `.github/workflows/build-windows-self-hosted.yml` — the Windows reference build (`qmake`/`nmake` against `Solution.pro` + each `SUBDIRS`-omitted `.pro` individually, smoke-launch step), run on a local machine or self-hosted Windows runner, not `windows-latest`.

## Implementation Steps
1. Re-run the coordinator's grep patterns per-file (not just per-module counts) for `windows.h`, `_WIN32`/`WIN32`, and the corrected real-COM pattern (`CoCreateInstance|QueryInterface|__uuidof|IID_|CComPtr|CoInitialize`) across every module — produce the raw per-file hit list.
2. For every WinAPI/COM-pattern hit, read the surrounding code (not just the grep line) to classify (a)/(b)/(c)/(d) with a cited `file:line` reason — apply the Modules-export-macro correction and the `modulemanager.cpp`/`QLibrary` finding from Key Insights as the template for "verify before trusting a bare grep count."
3. Specifically re-verify there is no OTHER category-(d) candidate hiding in the repo (the original "9 COM files" one turned out not to qualify) — grep the real COM pattern repo-wide one more time as a closing sanity check before finalizing the table.
4. Verify CEF's macOS SDK/framework availability (does CEF ship a mac binary distribution BAS can vendor? confirm via CEF's own release channel) and draft the `macx { ... }` qmake block shape needed in `ChromeWorker.pro`/`WebInterfaceBrowser.pro`/`SchedulerBrowser.pro`/`Engine.pro` — a checklist item, tracked separately from the Rust classification table since it's build-config, not Rust; feeds Phase 2 whenever it runs.
5. Provision the Windows reference build environment (Qt install + `basbuild` dependency bundle — currently blocked, plan.md Unresolved Questions #3/#5), then stand it up: local machine or self-hosted runner, `qmake`/`nmake` against `Solution.pro` + each omitted `.pro` individually, smoke-launch step. This is now the plan's primary validation checkpoint, referenced by every Phase-3-onward phase's Windows check, and doubles as Phase 2's pre-change baseline whenever that hardening track runs.
6. Write `docs/macos-blocker-classification.md` with the final per-file table + priority tiers (Architecture) — this becomes Phase 2's literal task list whenever Phase 2 is scheduled.

## Todo List
- [ ] Per-file grep sweep: `windows.h`, `_WIN32`/`WIN32`, corrected real-COM pattern
- [ ] Classify every hit (a/b/c/d) with cited `file:line` reasoning
- [ ] Repo-wide closing sanity check: confirm no other category-(d) candidate exists
- [ ] Verify CEF macOS SDK availability + draft `macx {}` qmake block shape
- [ ] Provision Windows build env (Qt + `basbuild` bundle) — currently blocked
- [ ] Stand up the Windows reference build (PRIMARY validation target for the whole plan)
- [ ] Write `docs/macos-blocker-classification.md`

## Success Criteria
- `docs/macos-blocker-classification.md` classifies every `windows.h`/`_WIN32`/WinAPI/COM-pattern hit repo-wide, each row with `file:line` evidence and a category — no "unclassified" rows left.
- The Modules-export-macro and `modulemanager.cpp`/`QLibrary` corrections (Key Insights) are captured in the deliverable as explicit "verified NOT category (d)" entries, not silently dropped — anyone reading the doc later sees the original claim AND why it was corrected.
- The Windows reference build produces a green build + smoke-launch — this IS the plan's primary success gate for standing up the migration (Phase 3 onward validates against it), not a side deliverable. Currently blocked pending Qt + `basbuild`-bundle provisioning.
- CEF-macOS checklist item has a documented answer (SDK available: yes/no, `macx {}` block drafted or blocked-on-what), ready for whenever Phase 2's hardening track is scheduled.

## Risk Assessment
- **Trusting a module-level grep count without reading the matched code** — this is exactly what produced the wrong "COM redesign" conclusion; mitigated by this phase's own corrected-finding discipline (Key Insights) and by requiring cited `file:line` evidence for every classification row, not a bare count.
- **CEF macOS SDK unavailable or requires a paid/restricted license** — if verification (step 4) finds this blocked, escalate to the user before Phase 2 assumes CEF-on-mac is solvable; this would change Phase 2's own scope (ChromeWorker/WebInterfaceBrowser/SchedulerBrowser might only reach "compiles" on mac, not "launches with working browser," until CEF is resolved) — legitimate dependency-conflict defer reason if it materializes.
- **The Windows reference build is now on the plan's critical path, and it's currently blocked by an unprovisioned environment** (no Qt, no `basbuild` bundle on this machine) — this is a real risk to the plan's whole timeline, not a scoped-down supporting-task concern anymore. Mitigation: resolve plan.md Unresolved Questions #3/#5 (Qt version + `basbuild` access) before Phase 3 is scheduled to start; don't let this phase's classification work substitute for actually unblocking the build.

## Security Considerations
- No code changes in this phase — no new attack surface. The classification table itself should flag (informationally, not as a fix requirement) any WinAPI call site that touches credentials/process-handles/crash-dumps, since Phase 2's fix for those needs the same security scrutiny the original plan applied per-module (e.g. `CrashHandler.cpp`'s dbghelp.dll use should not, post-fix, silently disable crash reporting on non-Windows without a documented decision).

## Test Strategy & Quality Gate
Lane `high-risk`.
- **Spec:** Goal — produce a verified, evidence-cited classification of every macOS-blocking call site repo-wide. AC: Given the repo at its current commit, When the classification grep+read process runs, Then every `windows.h`/`_WIN32`/COM-pattern hit has exactly one category with `file:line` evidence; Given the corrected Modules/`modulemanager.cpp` findings, When cross-checked against a fresh COM-pattern grep scoped to C/C++ source, Then zero real COM hits remain unaccounted for in BAS's own C/C++ code (vendored non-C++ assets like Monaco's `cpp.js` or Sequelize's `manifest.json` are explicitly out of scope for this check). I/O contract: N/A (investigation deliverable, not code). Out-of-scope: implementing any fix (Phase 2); resolving the exact redesign shape for any future category-(d) finding, if one is found, beyond scoping it.
- **Pyramid:** N/A (investigation phase — the phase's "test" is evidence-verification of its own classification claims, per the Evidence-execution discipline used throughout this plan's validation).
- **E2E scenario:** "Clean checkout on the provisioned Windows environment → `qmake`/`nmake` → smoke-launch → process-alive check" — the Windows reference build's own e2e check, which this phase owns and which every Phase-3-onward Windows checkpoint reuses. (The classification table itself has no e2e shape; it's a document.)
- **Coverage target:** N/A (no code).
- **Evidence commands:** the exact `grep -rn` invocations used for the sweep (windows.h, `_WIN32`/`WIN32`, real-COM pattern) with their output, pasted into `docs/macos-blocker-classification.md`'s methodology section — this IS the evidence for this phase's own claims, same discipline required of every classification row.

## ADR Rationale
**Decision:** Run a fresh, cited, per-file classification pass in this phase rather than taking the coordinator's module-level grep counts (including the "COM usage" framing) at face value into Phase 2's implementation order.
**Why:** the "COM usage: 9 Modules files, architecturally serious" claim was falsified with concrete evidence during this plan's own drafting (zero real COM APIs in any BAS C/C++ source, confirmed by a grep for `CoCreateInstance|QueryInterface|__uuidof|IID_|CComPtr|CoInitialize` scoped to `*.cpp`/`*.h`/`*.hpp`/`*.cc`) — building Phase 2 around a redesign that isn't needed would waste the plan's first and most schedule-sensitive milestone on a non-problem, and worse, would risk under-scoping the REAL Tier-1 item (ChromeWorker's 36 files). Per this plan's own Role Core discipline ("when the user heads the wrong way, say so directly with evidence"), the correction is made here, cited, and carried forward into Phase 2's re-ranked priority order.
**Alternatives rejected:** accept the coordinator's "Modules COM redesign, top priority" framing unverified (would misallocate Phase 2's effort and mischaracterize the plan's actual risk profile); silently reorder Phase 2 without documenting why (would look like an unexplained deviation from an explicit instruction, and would lose the evidence trail if the correction itself is later questioned).

## Next Steps
- Phase 2 (whenever scheduled — non-gating, may run parallel to or after Phase 3+) consumes `docs/macos-blocker-classification.md` as its literal task list, in the priority tiers this phase defines (ChromeWorker → Engine → Modules → smaller modules) — corrected from the coordinator's original Modules-first ordering.
- Phase 3 onward does NOT wait on this phase or on Phase 2 — it depends only on this phase's Windows reference build actually being green (the plan's primary gate), which in turn depends on resolving the open Qt/`basbuild` environment blocker (plan.md Unresolved Questions #3/#5).
- If step 3's closing sanity check DOES find a genuine category-(d) case somewhere this phase's first pass missed, record it in plan.md's Unresolved Questions before Phase 2 starts — Phase 2 should not begin implementation against a classification table with an open (d) case still unscoped.
