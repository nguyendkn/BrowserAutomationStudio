# Phase 02: Migrate macOS-Blocking Call Sites to Cross-Platform Rust

## Context Links
- `phase-01-windows-dependency-inventory-classification.md` — `docs/macos-blocker-classification.md`, this phase's literal task list, in priority tiers (ChromeWorker → Engine → Modules → smaller modules).
- `research/researcher-02-interop-tooling.md` — `cxx`/`cbindgen` for exposing small new Rust crates to still-unported C++ callers; panic safety at every boundary.
- `plan.md` — `bas-platform` crate definition, cross-platform-first Overview.

## Overview
- **Priority:** P1 — **this phase's Success Criteria is the plan's primary gate: a clean checkout builds and BAS launches on macOS.** Everything before this phase (investigation) and everything after (Phase 3 onward, the broader module-by-module migration) is secondary to reaching this milestone first, per the user's explicit reprioritization: "get the project to actually start on macOS" comes before the rest of the incremental migration continues.
- **Status:** pending. Depends on Phase 1's classification table.
- Scope: **narrow, surgical fixes only** — replace or platform-abstract the SPECIFIC blocking call sites Phase 1 identified, via small new Rust crates (`bas-platform` for portable primitives, a minimal loading-mechanism slice of `bas-engine-plugin` if Phase 1's closing sanity check found a real need — it did not, per Phase 1's correction) called from the STILL-UNPORTED C++ via `cxx`/`cbindgen`. This is explicitly NOT a full rewrite of Engine/ChromeWorker/Modules — that stays for Phase 4 onward. Also covers the CEF-macOS qmake build-config work (not Rust) needed for the same launch goal.

## Key Insights
- Phase 1's corrected classification changes this phase's priority order from the original "Modules first" framing: **Tier 1 is ChromeWorker's 36 `windows.h` files** (the largest real blocker), **Tier 2 is Engine's 14 `windows.h` files + 2 genuine `LoadLibrary` sites** (`CrashHandler.cpp` dbghelp.dll, `devicescalemanager.cpp` Shcore.dll) **+ the `OpenProcess`/`HANDLE` site** (`webdriverprocesscomunicatorfactory.cpp`) **+ `memoryinfo.cpp`'s `GlobalMemoryStatusEx` fallback upgrade**, **Tier 3 is Modules' TWO real `windows.h` files** (`Modules/UserNotification/dll/moduledll.cpp`, `Modules/Timezones/dll/maxminddb.c`) **+ the 9 trivial `__declspec`-only export headers** (cheap, batch together — no plugin-loader redesign needed, since `modulemanager.cpp`'s `QLibrary`-based loading is already cross-platform), **Tier 4 is the remaining smaller modules** (WebInterfaceBrowser 5, Studio 3, SchedulerBrowser 2, Scheduler 2, ShopViewer 1, RemoteExecuteScript 1). Note: `processdeleter.cpp`'s `windows.h` include is dead (category a) — removed alongside Tier 2, not a real fix.
- Because this phase touches call sites inside modules that are NOT yet fully ported to Rust (ChromeWorker/Engine/Modules full migration is Phase 6/8/11), the fix pattern here is deliberately small: a new `bas-platform` crate exposing cross-platform primitives (process info via `sysinfo`, portable export-macro equivalents, a `#[cfg(target_os)]`-gated trait for the genuinely OS-specific cases) via a `cbindgen`-generated C header, called from the UNCHANGED surrounding C++ at just the blocking line. This is narrower than a `cxx` bidirectional bridge because these call sites don't need C++ to call back into Rust — they just need a portable C function to call instead of a WinAPI one.
- CEF-on-macOS is a parallel, non-Rust track: Phase 1 found zero `macx {}` qmake blocks anywhere in the repo and `ChromeWorker.pro`'s `LIBS +=` line unconditionally links Windows system libs alongside CEF. This phase must add real `macx { }` blocks to `ChromeWorker.pro`, `WebInterfaceBrowser.pro`, `SchedulerBrowser.pro`, and `Engine.pro` with mac-appropriate CEF framework paths and mac system libs (dropping the Windows-only `-lAdvapi32 -luser32 -lPsapi -lshell32 -lDbgHelp -lComdlg32 -lgdi32` on that platform) — this is qmake/build-config work, verified against Phase 1's CEF-SDK-availability finding, not a code port.
- Golden-file characterization tests (per this plan's own hard constraint) must exist for each call site BEFORE it's touched — using Phase 1's Windows CI reference build as the pre-change behavior snapshot.

## Requirements
- Functional: every category-(b) call site (Phase 1) is replaced with a cross-platform crate/macro; every category-(c) call site is wrapped behind a `#[cfg(target_os)]`-gated trait in `bas-platform` with a real or explicitly-no-op alternative on macOS/Linux; category-(a) sites are removed or left unguarded (no behavior to preserve).
- Non-functional: for every call site touched, a before/after characterization test exists (behavior on Windows, via Phase 1's CI reference build, unchanged) before AND after the change — no call site is "fixed" without a regression check.
- CEF's macOS build-config work is verified against real CEF mac binaries (Phase 1's availability finding), not assumed to compile blind.
- **Primary gate:** `BrowserAutomationStudio.exe`'s macOS equivalent (whatever the qmake target produces on `macx`) builds from a clean checkout on a macOS CI runner (GitHub Actions `macos-latest`) or local macOS dev machine, and launches — even though the vast majority of Engine/ChromeWorker/Modules business logic is still unported C++. Only the specific blocking call sites need to be resolved for this gate; full-logic correctness on macOS (e.g. does every script function actually WORK on macOS) is explicitly out of scope until Phase 4 onward ports the real logic.

## Architecture
- **New crate `bas-platform`** (`rust/bas-platform/`): cross-platform OS primitives used by call sites across MULTIPLE still-C++ modules (justifies its own crate — a genuinely cross-cutting concern per the corrected repo-wide scan, not a fragment of any one module). Internal modules: `process` (via `sysinfo`, replacing any raw process-handle WinAPI), `export_macro` (a `cbindgen`-friendly portable dllexport equivalent, mostly a documentation/build-config change on the C++ side rather than Rust logic), `platform_trait` (the `#[cfg(target_os)]`-gated trait for genuinely OS-specific cases like `CrashHandler.cpp`'s crash-dump API and `devicescalemanager.cpp`'s DPI-scaling API — Windows arm uses the existing WinAPI logic ported as-is, macOS/Linux arm is a real equivalent where one exists or a documented no-op where none does).
- Exposed to still-C++ callers via `cbindgen`-generated C header (researcher-02's recommended direction for "self-contained Rust module → C++ caller") — no `cxx` bidirectional bridge needed here since C++ doesn't need to call back into anything stateful.
- `bas-platform` is later REUSED (not reimplemented) by Phase 6's `bas-chromeworker` and Phase 8's `bas-engine-core` `platform` submodules once those modules are fully ported — this phase's crate becomes their shared dependency, avoiding duplicate cross-platform-primitive code (DRY, per plan.md's crate-count discipline).
- CEF-macOS build-config: `macx { LIBS += -F<cef-framework-path> -framework "Chromium Embedded Framework" ... INCLUDEPATH += <cef-mac-sdk-include> }` blocks added per Phase 1's SDK-availability finding, alongside (not replacing) the existing Windows `LIBS +=` line.

## Related Code Files
- `/Users/nguyendk/Documents/projects/me/bas/Solution/ChromeWorker/` — modify: the 36 `windows.h`-including files, each blocking line replaced with a `bas-platform` call or `#ifdef` removal, per Phase 1's per-file classification (exact list from `docs/macos-blocker-classification.md`).
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/CrashHandler.cpp`, `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/devicescalemanager.cpp` — modify: wrap the `LoadLibrary`/`GetProcAddress` calls behind `bas-platform`'s `platform_trait`.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/webdriverprocesscomunicatorfactory.cpp` — modify: replace `OpenProcess`/`HANDLE` with a `bas-platform::process` call.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/memoryinfo.cpp` — modify: replace the hardcoded non-Windows fallback with a real `bas-platform`/`sysinfo`-backed value.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/processdeleter.cpp` — modify: delete the dead `windows.h` include (no other change — body is already portable).
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/` — modify: remaining 12 `windows.h` files + 6 `_WIN32` guard sites per Phase 1's classification.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Modules/UserNotification/dll/moduledll.cpp` — modify: one of the two real `windows.h` includes.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Modules/Timezones/dll/maxminddb.c` — modify: the second real `windows.h` include (also pulls in `ws2ipdef.h`); classify and fix per Phase 1's finding, don't assume trivial.
- `/Users/nguyendk/Documents/projects/me/bas/Solution/Modules/{DateTime,Timezones,Processes,FileSystem,ImageProcessing,FontPack,RegularExpression,UserNotification,CurlWrapper}/dll/moduledll.h` — modify: replace `__declspec(dllexport)` with a portable export macro (`#if defined(_WIN32) ... __declspec(dllexport) ... #else ... __attribute__((visibility("default"))) ... #endif`).
- `/Users/nguyendk/Documents/projects/me/bas/Solution/WebInterfaceBrowser/`, `/Users/nguyendk/Documents/projects/me/bas/Solution/Studio/`, `/Users/nguyendk/Documents/projects/me/bas/Solution/SchedulerBrowser/`, `/Users/nguyendk/Documents/projects/me/bas/Solution/Scheduler/`, `/Users/nguyendk/Documents/projects/me/bas/Solution/ShopViewer/`, `/Users/nguyendk/Documents/projects/me/bas/Solution/RemoteExecuteScript/` — modify: Tier 4's small remaining call sites (5/3/2/2/1/1 files respectively).
- `/Users/nguyendk/Documents/projects/me/bas/Solution/ChromeWorker/ChromeWorker.pro`, `/Users/nguyendk/Documents/projects/me/bas/Solution/WebInterfaceBrowser/WebInterfaceBrowser.pro`, `/Users/nguyendk/Documents/projects/me/bas/Solution/SchedulerBrowser/SchedulerBrowser.pro`, `/Users/nguyendk/Documents/projects/me/bas/Solution/Engine/Engine.pro` — modify: add `macx { }` qmake blocks.
- **Create:** `/Users/nguyendk/Documents/projects/me/bas/rust/Cargo.toml`, `/Users/nguyendk/Documents/projects/me/bas/rust/bas-platform/{Cargo.toml,src/lib.rs,src/process.rs,src/platform_trait.rs,src/c_abi.rs,cbindgen.toml}` — the Rust workspace's first crate (predates Phase 4's originally-first `bas-contracts`/`bas-browser-shell`; `bas-platform` is now the actual first crate in execution order, though plan.md's architecture table lists it alongside the other 8 for responsibility purposes).
- **Create:** `/Users/nguyendk/Documents/projects/me/bas/.github/workflows/build-macos.yml` — new macOS CI workflow (the phase's primary gate lives here).

## Implementation Steps
1. Stand up a `macos-latest` GitHub Actions workflow skeleton that attempts today's unmodified build (expected to FAIL — this captures the exact current failure point as a starting reference, not a success gate yet).
2. For each Tier-1 (ChromeWorker) call site: write a golden characterization test against the Windows CI reference build (Phase 1) capturing current behavior, then replace the call site with `bas-platform` or a portable equivalent, then re-run the golden test against the (still Windows-built) result to confirm no behavior change, THEN attempt the macOS build again — expect the macOS build to progress further each time a blocking site is removed.
3. Repeat step 2's discipline for Tier 2 (Engine), Tier 3 (Modules), Tier 4 (smaller modules), in that priority order.
4. Add the `macx { }` qmake blocks (Architecture) to `ChromeWorker.pro`, `WebInterfaceBrowser.pro`, `SchedulerBrowser.pro`, `Engine.pro`, using Phase 1's verified CEF-mac-SDK paths.
5. Once all Tier 1-4 call sites are resolved and CEF's mac build-config is in place, run a full clean-checkout build on the `macos-latest` CI workflow; iterate on any remaining compile error (expected — qmake/mac toolchain quirks are likely even after the code-level fixes).
6. Add a smoke-launch step on macOS (same pattern as the original Windows smoke-launch: short timeout, process-alive/window-creation check) — this is the phase's primary gate, run it to green.
7. Document every fix applied (call site → category → fix) in `docs/macos-blocker-classification.md`, updating each row's status from "classified" to "fixed, verified."

## Todo List
- [ ] `macos-latest` CI skeleton, capture current failure point as baseline
- [ ] Tier 1 (ChromeWorker, 36 files): golden test → fix via `bas-platform` → re-verify → macOS build progress check
- [ ] Tier 2 (Engine, 14 `windows.h` + 2 `LoadLibrary` + 1 `OpenProcess` + 1 `memoryinfo.cpp` fallback files): same discipline
- [ ] Tier 3 (Modules, 2 `windows.h` + 9 `__declspec`-export files): same discipline
- [ ] Tier 4 (smaller modules, ~14 files total): same discipline
- [ ] `macx {}` qmake blocks: ChromeWorker/WebInterfaceBrowser/SchedulerBrowser/Engine
- [ ] Full clean-checkout macOS CI build green
- [ ] macOS smoke-launch step green — **the plan's primary gate**
- [ ] Update `docs/macos-blocker-classification.md` status column

## Success Criteria
- **A clean checkout builds and BAS (or its qmake-defined macOS launch target) launches on macOS** — evidenced by a green `macos-latest` CI run with a passing smoke-launch step. This is the plan's primary milestone; everything in Phase 3 onward assumes this is true.
- Every Tier 1-4 call site has a passing before/after characterization test (via the Windows CI reference build) proving the fix didn't change Windows behavior.
- CEF's macOS build-config compiles and links against real CEF mac binaries (not a stub) — evidenced by the mac build actually invoking CEF APIs, not just skipping that code path.
- `bas-platform` builds and unit-tests on macOS + Linux in CI — this crate's own cross-platform readiness checkpoint, and the foundation Phase 6/8 reuse.

## Risk Assessment
- **A category-(c) call site has no real macOS equivalent and BAS's behavior genuinely degrades there** (e.g. `CrashHandler.cpp`'s Windows crash-dump mechanism) — mitigated by an explicit, documented no-op or reduced-functionality fallback (e.g. macOS gets a basic signal-handler-based crash log instead of `dbghelp.dll`'s minidump) rather than silently pretending parity; record the gap in `docs/macos-blocker-classification.md`, don't hide it.
- **CEF mac SDK licensing/availability blocks the build-config track** — if Phase 1 found this unresolved, this phase cannot fully complete its gate; escalate to the user rather than shipping a macOS build that compiles but can't actually launch a browser (legitimate dependency-conflict defer reason, per this plan's own escalation rules).
- **Fixing 36+ ChromeWorker call sites individually risks touching behavior beyond the blocking line** — mitigated by keeping each fix to the SMALLEST possible diff (replace exactly the WinAPI call, not surrounding logic) and the per-call-site golden test in step 2, not a batch fix-then-test-everything-at-the-end approach.
- **This phase's macOS build success doesn't mean the APP works on macOS** — only the specific blockers are resolved; Engine/ChromeWorker/Modules business logic is still unported C++ and may behave incorrectly at runtime for reasons unrelated to WinAPI (e.g. file-path assumptions, case-sensitivity). This is explicitly accepted scope (Overview) — full functional correctness on macOS is Phase 4 onward's job, not this phase's gate.

## Security Considerations
- `bas-platform`'s `c_abi.rs` (the `cbindgen`-exposed surface called from unmodified C++) is a new `unsafe` concentration point — same containment discipline as every other phase's `platform`/`c_abi` module: confine `unsafe`, document ownership of every pointer crossing the boundary.
- `CrashHandler.cpp`'s fallback behavior on macOS (Risk Assessment) must not silently disable crash reporting without a recorded decision — a security-relevant regression if crash telemetry feeds into an incident-response process.
- The portable export-macro fix for Modules (`__attribute__((visibility("default")))`) changes symbol visibility semantics on POSIX — verify it doesn't inadvertently export MORE symbols than the Windows `__declspec(dllexport)` did (e.g. an explicit visibility-hidden default for the rest of the translation unit, matching current Windows behavior where only `dllexport`-marked symbols are visible).

## Test Strategy & Quality Gate
Lane `high-risk`.
- **Spec:** Goal — resolve every macOS-blocking call site with the smallest possible diff, reach a working macOS build+launch. AC: Given Phase 1's classification table, When each call site's fix is applied, Then its before/after characterization test passes (no Windows behavior change) AND the macOS build progresses past that blocker; Given all Tier 1-4 fixes + CEF mac build-config, When a clean checkout builds on `macos-latest` CI, Then the app launches and the smoke-launch step passes. I/O contract: N/A per call site (behavior-preserving point fixes); the macOS launch target's process-alive/window-creation signal is the aggregate I/O contract for the phase gate. Out-of-scope: full business-logic correctness on macOS, any category-(d) redesign (none found per Phase 1), full ChromeWorker/Engine/Modules migration (Phase 4+).
- **Pyramid:** ~60% unit (`bas-platform`'s own Rust tests), ~20% integration (per-call-site before/after characterization against the Windows reference build), ~20% e2e (the macOS build+smoke-launch gate itself).
- **E2E scenario:** "Clean checkout on macOS CI → build → launch → process-alive check" — the phase's (and the plan's) primary e2e gate.
- **Coverage target:** ≥90% line / ≥75% branch diff-coverage on `bas-platform`'s new Rust code, via `cargo tarpaulin`. The C++-side point fixes are covered by the before/after characterization tests, not a Rust coverage number (most of the diff is C++ call-site edits, not new Rust logic).
- **Evidence commands:** `macos-latest` CI run log (build + smoke-launch pass), each Tier's before/after characterization diff output, `cargo test -p bas-platform`, `cargo tarpaulin -p bas-platform --out Html`.

## ADR Rationale
**Decision:** Fix the macOS-blocking call sites via small, targeted new Rust crates (`bas-platform`) called from otherwise-unported C++, rather than either (a) fixing them in pure C++ with `#ifdef` guards and no Rust at all, or (b) waiting until each module's full Rust port (Phase 4+) to address them.
**Why:** the user's explicit goal is "start được project" on macOS as the FIRST milestone, with the fuller migration continuing afterward — waiting for Phase 6/8/11's full ports would push the macOS-launch milestone months out, defeating the reprioritization's purpose. Pure-C++ `#ifdef` fixes would achieve the same macOS-launch goal without any Rust, but would NOT advance this plan's parallel, equally-real goal (per plan.md's Overview) of moving cross-platform logic into Rust — and would mean re-doing the same call sites in Rust later anyway once Phase 6/8 port their surrounding modules. A small `bas-platform` crate gets both: the macOS-launch milestone now, AND a reusable cross-platform-primitives crate Phase 6/8 build on instead of reinventing.
**Alternatives rejected:** pure C++ `#ifdef` fixes with no Rust (achieves the launch goal but wastes the opportunity to seed `bas-platform` early, meaning Phase 6/8 would duplicate this cross-platform-primitive work later); defer all WinAPI fixes to each module's full Rust port (rejected — misses the user's explicit "unblock macOS first" reprioritization, pushes the milestone out by months).

## Next Steps
- Phase 3 onward (the broader module-by-module migration: browser-shell PoC, scheduling utilities, ChromeWorker full migration, Engine discovery+sub-crates, Modules) proceeds with macOS already buildable — each subsequent phase's "cross-platform CI checkpoint" (per plan.md's Overview) is now verifying against a macOS target that ALREADY launches, not one that's never been tried.
- Phase 6 (`bas-chromeworker` full migration) and Phase 8 (`bas-engine-core` full migration) both depend on `bas-platform` — reuse it for their `platform` submodules rather than reintroducing `windows-rs`-first designs; update those phase files' Architecture sections to reference `bas-platform` as an existing dependency, not a from-scratch submodule (see cross-references added to phase-06/phase-08).
- Phase 11 (`bas-modules` migration) inherits the already-fixed `__declspec` export macros and the already-cross-platform `QLibrary`-based loader from this phase — its own scope shrinks accordingly (no loader redesign needed, confirmed by Phase 1's correction).
